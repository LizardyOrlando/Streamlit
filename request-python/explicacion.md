# Se debe instalar el request de esta forma:
## python -m pip install requests
### se debe llamar a la libreria con import request
### con request.get se obtiene la url y con request.text se muestra la informacion
